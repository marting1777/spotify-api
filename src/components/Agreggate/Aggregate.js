import React, {Component} from 'react'
import classes from './Aggregate.css'

class Aggregate extends Component {
    render () {
        return (
            <div className={classes.Menu}>
                <h2 className={classes.Titulo}>Next Text</h2>
            </div>
        )
    }
}

export default Aggregate